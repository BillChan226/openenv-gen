# Backend environment variables
# Copy to .env and adjust as needed.

# Server
PORT=8080
HOST=0.0.0.0

# CORS (comma-separated). Example: http://localhost:3000
# CORS_ORIGIN=http://localhost:3000

# Database
# If running via docker-compose, DB_HOST should usually be 'db'
# If running locally, prefer DB_HOST=127.0.0.1 to avoid IPv6 ::1 resolution issues
DB_HOST=127.0.0.1

# Optional: enable in-memory auth fallback when DB isn't running
# ALLOW_NO_DB_AUTH=true
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=voyager
