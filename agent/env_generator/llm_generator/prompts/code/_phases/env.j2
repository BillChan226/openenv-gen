<ENV_PYTHON_GUIDANCE>
CRITICAL (Python on macOS): `python` may not exist. Prefer `python3` for running Python commands.

Examples:
- execute_bash("python3 -m py_compile env/client.py env/example_usage.py")
- execute_bash("python3 -m pip install -r env/requirements.txt")
</ENV_PYTHON_GUIDANCE>
